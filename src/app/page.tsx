import Header from "@/components/Header";
import WalletConnected from "@/components/WalletConnected";

const page = () => {
    return (
        <div>
            <Header />
            <WalletConnected />
        </div>
    );
};

export default page;
